const uppercase = require('../functions');

describe('Do the socket functions work?', () => {

  it('turns a string into uppercase', () => {
    expect(uppercase('sadmodude')).toBe('SADMODUDE');
  });

  it('reads a file', () => {

  });

  it('writes a file', () => {

  });


});